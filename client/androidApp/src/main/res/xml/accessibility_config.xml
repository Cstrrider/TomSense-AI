<?xml version="1.0" encoding="utf-8"?>
<!--
  Screen read. canRetrieveWindowContent is the capability that makes
  "what does this say?" work on content the assist API doesn't expose.

  This is the most invasive permission on Android. The service is declared
  but inert: it does nothing until the user enables it in system settings,
  and even then it reads on demand rather than continuously.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="300"
    android:description="@string/accessibility_description" />
