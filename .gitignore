# env / secrets
.env

# per-install runtime config (generated/edited by the backend or operator)
.mounts.json
docker-compose.override.yml
deploy-targets.json

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# node / sveltekit (phase 2)
node_modules/
.svelte-kit/
build/

# editor
.vscode/
.idea/

# generated
*.log
generated_images/

# next.js build output
.next/

# capacitor: regenerated by `npx cap sync` with the instance URL baked in
frontend/android/app/src/main/assets/capacitor.config.json

# release signing (keystore + passwords) — NEVER commit
signing/
