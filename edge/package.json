{
  "name": "tomsense-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate:local": "wrangler d1 migrations apply tomsense --local",
    "migrate:remote": "wrangler d1 migrations apply tomsense --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250900.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
